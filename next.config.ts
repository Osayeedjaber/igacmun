import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Uncomment the following if you use external images (e.g. from Supabase)
  // images: {
  //   remotePatterns: [
  //     {
  //       protocol: 'https',
  //       hostname: 'your-project.supabase.co',
  //     },
  //   ],
  // },
};

export default nextConfig;
